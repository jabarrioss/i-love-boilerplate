# assets/images

PNG, JPG, and other raster art.

Reference files here from `config/audio.lua` under `images` or load
directly via `assets:load("image", "name", "assets/images/foo.png")`.

Naming convention: `kebab-case` is fine, just be consistent.
